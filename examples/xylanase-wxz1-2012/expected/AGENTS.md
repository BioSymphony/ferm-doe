# Example Handoff

This directory is a public synthetic BioSymphony Ferm DoE example.

Use `campaign_manifest.json`, `inputs/`, and `expected/readiness_summary.json` as planning fixtures only.
Do not claim physical execution, assay validation, or production readiness from this example.
